<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pets available</title>
    <style>
        .text-lg {
            font-size: 35px; 
        }
      
        .bold-text {
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center align-items-center">
            <div class="col-md-6">
                <div class="col-md-12 bg-primary text-white border rounded p-3">
                    <p class="text-lg">Name: {{petData.pet_name}}</p>
                    <p class="text-lg">Species: {{petData.category.category_name}}</p>
                    <p class="text-lg">Breed: {{petData.breed}}</p>
                    <p class="text-lg">Adoption Centre: {{petData.adoption_center.center_name}}</p>
                    <p class="text-lg">Address: {{petData.adoption_center.address}}</p>
                    <p class="text-lg">Age: {{petData.age}}</p>
                </div>
            </div>
            <div class="col-md-6">
                <div class="col-md-12">
                    <img src="{{petData.image}}" alt="{{petData.name}}" class="img-fluid">
                </div>
            </div>
        </div>
    </div>
</body>
</html>