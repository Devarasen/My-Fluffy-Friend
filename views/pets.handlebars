{{!-- 
<body>
    <h1>All Pets</h1>
    <ul>
        {{#each petData}}
            <li><a href="/petRoutes/{{id}}">{{pet_name}}</a></li>
        {{/each}}
    </ul>
</body> --}}

<body>
    <div class="container">
        <div class="row justify-content-center align-items-center" style="height: 100vh;">
            {{#each petData}}
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card" style="width: 300px;">
                    <a href="/petRoutes/{{id}}">
                        <img src="{{image}}" class="card-img-top" alt="{{pet_name}}" style="height: 300px;">
                    </a>
                    <div class="card-body text-center">
                        <h5 class="card-title">{{pet_name}}</h5>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</body>